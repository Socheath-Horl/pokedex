import 'package:flutter/material.dart';

class TypeEffectScreen extends StatelessWidget {
  const TypeEffectScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Splash screen'));
  }
}
